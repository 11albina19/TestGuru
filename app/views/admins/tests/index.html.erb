<div class="container mt-2">
  <h1 class="mb-4"><%= t('.header') %></h1>
  <p>
    <%= link_to t('.create'), new_admins_test_path, class: "btn btn-primary" %>
    <%= link_to t('.all_gist'), new_admins_test_path, class: "btn btn-primary" %>
  </p>

  <table class="table table-striped table-hover">
    <thead class="table-dark">
    <tr>
      <th><%= t('.title') %></th>
      <th><%= t('.level') %></th>
      <th><%= t('.questions') %></th>
      <th colspan="3"><%= t('.actions') %></th>
    </tr>
    </thead>
    <tbody>
    <%= render @tests %>
    </tbody>
  </table>
</div>
